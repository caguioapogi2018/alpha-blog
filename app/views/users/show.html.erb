<h1><center> Welcome to <%= @user.username %>'s page</center></h1>
<div class="row">
 <div class="col-md-4 col-md-offset-4 center">
      <%= gravatar_for(@user, size: 100) %>
  </div>
</div>

<h4><center><%= @user.username %>'s Article's</center></h4>
<%= render 'articles/article', obj: @user.articles %>
